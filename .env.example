# Environment Variables для Vercel
# Скопіюйте це у Vercel Dashboard → Settings → Environment Variables

# Node.js версія (опціонально, Vercel автоматично виявить)
NODE_VERSION=20

# Build налаштування (вже в vercel.json, але можна перевизначити)
# BUILD_COMMAND=npm run build
# OUTPUT_DIRECTORY=build/client

# Для Analytics (опціонально)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Для custom domain (після деплою)
# VERCEL_URL=your-custom-domain.com

# Puter.js налаштування (якщо потрібні)
# Цей проект використовує Puter.js, який не потребує додаткових env змінних
